<div class="wrapper decidim-donations">
  <div class="row">
    <div class="row">
      <div class="columns large-8 large-centered text-center page-title">
        <h1><%= t(".title") %></h1>
        <h4><%= t(".instructions") %></h4>
      </div>
    </div>

    <% if Decidim::Donations::provider == :stripe_express %>
      <%= cell("decidim/donations/stripe_form", @form) %>
    <% else %>
      <%= cell("decidim/donations/amount_form", @form) %>
    <% end %>
  </div>
</div>
